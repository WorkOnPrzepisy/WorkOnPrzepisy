//data transfer via href
/*
const moreRecipe = document.querySelector('main'); // main div must have class="main" !!

console.log(moreRecipe);

moreRecipe.addEventListener('click', e => {
    if (e.target.tagName === "BUTTON" && e.target.classList.contains("button-more-recipe")) {
        console.log(e.target.classList[0]);
        const whichButton = moreRecipe.querySelector(`.${e.target.classList[0]}`).parentElement;
        const hrefInA = whichButton.href;
        const idInMeals =
            console.log(hrefInA);
    }
})
*/